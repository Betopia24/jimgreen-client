import { graphData } from "./__comp/graphData";
import SaturationDashboard from "./__comp/ThreeD";

const page = () => {
  return <div>{/* <SaturationDashboard apiResponse={graphData} /> */}</div>;
};

export default page;
